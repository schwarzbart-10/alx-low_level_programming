#include <stdio.h>
/**
 * main - runs all the code
 *
 * Description: Using the prinf function to print strings
 * Main: Displays the value of the prinf function
 * Return: return 0 to the exit function
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");
	return (0);
}
